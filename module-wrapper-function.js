(function (exports, require, module, __filename, __dirname) {
    //node doesn't execute our code directly
    //always wraps code inside each module in this block
    //whatever you write that goes into this block by default in node

    //see the require - it appears to be global but it actually is local to a module
})

//the above is a immemdiately invoked function in JS